{
  "scripts": {
    "serve": "bundle exec jekyll serve --livereload",
    "build": "env JEKYLL_ENV='production' bundle exec jekyll build"

  },
  "devDependencies": {
    "autoprefixer": "^9.6.1",
    "postcss-cli": "^6.1.3",
    "postcss-import": "^12.0.1",
    "purgecss": "^1.4.0",
    "tailwindcss": "^1.1.2"
  }
}
